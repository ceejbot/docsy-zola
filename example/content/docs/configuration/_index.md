+++
title = "Configuration"
description = "Every key the templates read, page-level front matter, and how to extend a template."
weight = 30
sort_by = "weight"
+++

The theme's full configuration contract lives in its `theme.toml`, with each
key's default and a comment. This section walks through the same keys grouped by
what they affect. Copy only the ones you change into your site's `config.toml`
under `[extra]`.
